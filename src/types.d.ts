type Theme = 'light' | 'dark'

type ThemeColors = {
  primary: string
  background: string
  text: string
  buttonBackground: string
  buttonText: string
  toggleBackground: string
  toggleKnob: string
  shadow: string
}
